# URL du backend Django (sans le /api final).
# En dev, le proxy Vite (vite.config.ts) redirige déjà /api vers cette URL,
# donc VITE_API_URL peut rester vide pour utiliser le proxy relatif.
VITE_API_URL=http://localhost:8000
